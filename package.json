{
  "name": "prime-cloud-monorepo",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "npm --prefix prime-cloud install && npm --prefix prime-cloud run build && npm --prefix prime-cloud/deploy install --production"
  }
}
